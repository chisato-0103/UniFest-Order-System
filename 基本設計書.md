# UniFest オーダー管理システム 基本設計書

## 目次

- [概要](#概要)
- [システム構成](#システム構成)
- [アーキテクチャ設計](#アーキテクチャ設計)
- [フロントエンド設計](#フロントエンド設計)
- [バックエンド設計](#バックエンド設計)
- [データベース設計](#データベース設計)
- [QRコード機能設計](#qrコード機能設計)
- [リアルタイム通信設計](#リアルタイム通信設計)
- [セキュリティ設計](#セキュリティ設計)
- [API設計](#api設計)
- [デプロイ設計](#デプロイ設計)
- [パフォーマンス設計](#パフォーマンス設計)
- [エラーハンドリング設計](#エラーハンドリング設計)

## 概要

本基本設計書は、UniFest オーダー管理システムの技術的詳細設計を定義する。React + TypeScript + Node.js + PostgreSQL の構成で、QRコード読み取り機能を中心とした店舗運営支援システムを実現する。

### 設計方針

- **モダンな技術スタック**: React 18 + TypeScript 5 による型安全な開発
- **高性能**: Vite ビルドツール、React仮想DOM最適化
- **リアルタイム性**: Socket.io による即座のデータ同期
- **モバイルファースト**: iOS Safari完全対応のレスポンシブ設計
- **開発効率**: TypeScript による開発時エラー検出
- **運用安定性**: PostgreSQL による永続化、自動バックアップ

## システム構成

### 全体構成図

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Frontend      │    │   Backend       │    │   Database      │
│   (React)       │◄──►│   (Node.js)     │◄──►│  (PostgreSQL)   │
│                 │    │                 │    │                 │
│ ┌─────────────┐ │    │ ┌─────────────┐ │    │ ┌─────────────┐ │
│ │  Components │ │    │ │   Express   │ │    │ │   Orders    │ │
│ │  - QRScanner│ │    │ │   Routes    │ │    │ │   Products  │ │
│ │  - OrderCard│ │    │ │             │ │    │ │   Settings  │ │
│ │  - Kitchen  │ │    │ ┌─────────────┐ │    │ └─────────────┘ │
│ └─────────────┘ │    │ │  Socket.io  │ │    └─────────────────┘
│                 │    │ │   Server    │ │
│ ┌─────────────┐ │    │ └─────────────┘ │
│ │  Services   │ │    └─────────────────┘
│ │  - API      │ │
│ │  - Socket   │ │
│ └─────────────┘ │
└─────────────────┘
```

### デプロイ構成

```
┌─────────────────────────────────────────────────────────────────┐
│                          Render Platform                        │
├─────────────────┬─────────────────┬─────────────────────────────┤
│  Static Sites   │  Web Services   │      PostgreSQL             │
│  (Frontend)     │  (Backend)      │      (Database)             │
│                 │                 │                             │
│  React Build    │  Node.js +      │  ┌─────────────────────────┐ │
│  + Vite         │  Express +      │  │  - 1GB Storage          │ │
│  (無料)         │  Socket.io      │  │  - SSL/TLS Encryption   │ │
│                 │  (750h/月 無料) │  │  - Auto Backup          │ │
└─────────────────┴─────────────────┴─────────────────────────────┘
```

## アーキテクチャ設計

### フロントエンド アーキテクチャ

```
src/
├── components/          # UIコンポーネント
│   ├── common/         # 共通コンポーネント
│   ├── order/          # 注文関連
│   ├── kitchen/        # 厨房関連
│   ├── delivery/       # 受け渡し関連
│   └── qr/            # QRコード関連
├── pages/              # ページコンポーネント
├── services/           # API通信・ビジネスロジック
├── hooks/              # カスタムHook
├── contexts/           # React Context
├── types/              # TypeScript型定義
├── utils/              # ユーティリティ関数
└── assets/             # 静的ファイル
```

### バックエンド アーキテクチャ

```
src/
├── routes/             # APIルート定義
├── controllers/        # ビジネスロジック
├── services/           # データベース操作
├── models/             # データモデル
├── middleware/         # ミドルウェア
├── socket/             # Socket.io関連
├── database/           # DB接続・初期化
├── utils/              # ユーティリティ
└── types/              # TypeScript型定義
```

## フロントエンド設計

### React コンポーネント設計

#### コンポーネント階層

```
App
├── OrderPage              # 注文画面
│   ├── ProductList        # 商品一覧
│   ├── ShoppingCart       # カート
│   └── QRCodeGenerator    # QRコード生成
├── KitchenPage            # 厨房画面
│   ├── OrderQueue         # 注文キュー
│   ├── CookingTimer       # 調理タイマー
│   └── OrderStatusCard    # 注文状況カード
├── DeliveryPage           # 受け渡し画面
│   ├── QRScanner          # QRスキャナー
│   ├── ReadyOrdersList    # 受け渡し待ち一覧
│   └── HandoverConfirm    # 受け渡し確認
└── AdminPage              # 管理画面
    ├── ProductManagement  # 商品管理
    ├── SystemSettings     # システム設定
    └── Analytics          # 分析画面
```

#### 状態管理設計

```typescript
// Context API による状態管理
interface AppState {
  orders: Order[];
  products: Product[];
  settings: SystemSettings;
  notifications: Notification[];
  socketConnection: boolean;
}

// カスタムHook例
const useOrders = () => {
  const { orders, setOrders } = useContext(AppContext);
  
  const createOrder = useCallback(async (orderData: CreateOrderRequest) => {
    const newOrder = await OrderService.createOrder(orderData);
    setOrders(prev => [...prev, newOrder]);
    return newOrder;
  }, [setOrders]);

  return { orders, createOrder };
};
```

### TypeScript 型定義

```typescript
// 注文関連の型定義
interface Order {
  orderId: string;
  orderNumber: string;
  customerId?: string;
  items: OrderItem[];
  totalAmount: number;
  status: OrderStatus;
  paymentStatus: PaymentStatus;
  createdAt: Date;
  updatedAt: Date;
  qrCode?: string;
  estimatedPickupTime?: Date;
}

interface OrderItem {
  productId: string;
  productName: string;
  quantity: number;
  unitPrice: number;
  toppings: Topping[];
  subtotal: number;
}

type OrderStatus = 
  | '注文受付' 
  | '調理待ち' 
  | '調理中' 
  | '調理完了' 
  | '受け渡し済み' 
  | 'キャンセル';

// QRコード関連の型定義
interface QRScanResult {
  data: string;
  timestamp: Date;
  method: 'camera' | 'manual';
}

interface QRCodeData {
  type: 'pickup' | 'order';
  orderNumber: string;
  timestamp: string;
  checksum?: string;
}
```

## バックエンド設計

### Express ルート設計

```typescript
// APIルート構成
app.use('/api/orders', ordersRouter);      // 注文管理
app.use('/api/products', productsRouter);  // 商品管理
app.use('/api/categories', categoriesRouter); // カテゴリ管理
app.use('/api/toppings', toppingsRouter);  // トッピング管理
app.use('/api/settings', settingsRouter); // システム設定
app.use('/api/stats', statsRouter);       // 統計・分析
app.use('/api/stock', stockRouter);       // 在庫管理
app.use('/api/emergency', emergencyRouter); // 緊急時対応

// ヘルスチェック
app.get('/api/health', healthController);
```

### レート制限設計

```typescript
// 開発・本番環境別レート制限
const createRateLimit = () => {
  const isDevelopment = process.env.NODE_ENV === 'development';
  
  return rateLimit({
    windowMs: isDevelopment ? 5 * 60 * 1000 : 15 * 60 * 1000, // 開発: 5分, 本番: 15分
    max: isDevelopment ? 1000 : 100,  // 開発: 1000回, 本番: 100回
    skip: (req) => {
      // localhost からのアクセスは制限しない
      const isLocalhost = req.ip === '127.0.0.1' || req.ip === '::1';
      return isDevelopment && isLocalhost;
    },
    message: {
      error: "Too many requests from this IP, please try again later."
    }
  });
};
```

### エラーハンドリング設計

```typescript
// 統一エラーハンドラー
class AppError extends Error {
  public readonly statusCode: number;
  public readonly isOperational: boolean;

  constructor(message: string, statusCode: number = 500, isOperational: boolean = true) {
    super(message);
    this.statusCode = statusCode;
    this.isOperational = isOperational;
    Error.captureStackTrace(this, this.constructor);
  }
}

// エラーミドルウェア
const errorHandler = (err: Error, req: Request, res: Response, next: NextFunction) => {
  if (err instanceof AppError) {
    res.status(err.statusCode).json({
      error: {
        message: err.message,
        status: err.statusCode,
        timestamp: new Date().toISOString()
      }
    });
  } else {
    // 予期しないエラー
    console.error('Unexpected error:', err);
    res.status(500).json({
      error: {
        message: 'Internal Server Error',
        status: 500,
        timestamp: new Date().toISOString()
      }
    });
  }
};
```

## データベース設計

### PostgreSQL スキーマ設計

```sql
-- 注文テーブル（拡張版）
CREATE TABLE orders (
    order_id SERIAL PRIMARY KEY,
    customer_id INTEGER,
    order_number VARCHAR(4) NOT NULL UNIQUE,
    total_amount DECIMAL(10,2) NOT NULL,
    status VARCHAR(20) NOT NULL DEFAULT '注文受付'
        CHECK (status IN ('注文受付', '調理待ち', '調理中', '調理完了', '受け取り済み', 'キャンセル')),
    payment_status VARCHAR(20) NOT NULL DEFAULT '未払い'
        CHECK (payment_status IN ('未払い', '支払済み')),
    payment_method VARCHAR(20) DEFAULT '現金'
        CHECK (payment_method IN ('現金', 'クレジットカード', 'PayPay', 'その他')),
    estimated_pickup_time TIMESTAMP,
    actual_pickup_time TIMESTAMP,
    special_instructions TEXT,
    cooking_start_time TIMESTAMP,
    cooking_completion_time TIMESTAMP,
    cancel_reason TEXT,
    qr_code TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 注文明細テーブル
CREATE TABLE order_items (
    item_id SERIAL PRIMARY KEY,
    order_id INTEGER REFERENCES orders(order_id) ON DELETE CASCADE,
    product_id INTEGER REFERENCES products(product_id),
    product_name VARCHAR(100) NOT NULL,
    quantity INTEGER NOT NULL CHECK (quantity > 0),
    unit_price DECIMAL(10,2) NOT NULL,
    subtotal DECIMAL(10,2) NOT NULL,
    toppings JSONB DEFAULT '[]'::jsonb,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- トリガー関数（updated_at自動更新）
CREATE OR REPLACE FUNCTION update_updated_at_column()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = CURRENT_TIMESTAMP;
    RETURN NEW;
END;
$$ language 'plpgsql';

-- トリガー設定
CREATE TRIGGER update_orders_updated_at 
    BEFORE UPDATE ON orders 
    FOR EACH ROW 
    EXECUTE FUNCTION update_updated_at_column();
```

### インデックス設計

```sql
-- パフォーマンス最適化インデックス
CREATE INDEX CONCURRENTLY idx_orders_order_number ON orders(order_number);
CREATE INDEX CONCURRENTLY idx_orders_status ON orders(status);
CREATE INDEX CONCURRENTLY idx_orders_created_at ON orders(created_at);
CREATE INDEX CONCURRENTLY idx_order_items_order_id ON order_items(order_id);
CREATE INDEX CONCURRENTLY idx_products_category_status ON products(category_id, status);

-- 複合インデックス
CREATE INDEX CONCURRENTLY idx_orders_status_created ON orders(status, created_at);
CREATE INDEX CONCURRENTLY idx_orders_pickup_time ON orders(estimated_pickup_time) 
    WHERE status IN ('調理完了', '受け取り済み');
```

## QRコード機能設計

### QRスキャナー アーキテクチャ

```typescript
// QRScannerコンポーネント設計
interface QRScannerProps {
  open: boolean;
  onClose: () => void;
  onScan: (data: string) => void;
  title?: string;
  enableManualInput?: boolean;
}

const QRScanner: React.FC<QRScannerProps> = ({
  open, onClose, onScan, title, enableManualInput = true
}) => {
  // html5-qrcode設定
  const qrCodeConfig = {
    fps: 10,
    qrbox: { width: 250, height: 250 },
    aspectRatio: 1.0,
    experimentalFeatures: {
      useBarCodeDetectorIfSupported: true  // ブラウザネイティブAPI使用
    },
    // iOS Safari対応
    videoConstraints: {
      facingMode: "environment",  // 背面カメラ優先
      width: { min: 640, ideal: 1280, max: 1920 },
      height: { min: 480, ideal: 720, max: 1080 }
    }
  };
  
  // エラーハンドリング
  const handleScanError = (error: unknown) => {
    const errorMessage = error instanceof Error ? error.message : "不明なエラー";
    console.warn("QRスキャンエラー:", errorMessage);
    
    // iOS Safari特有のエラー対応
    if (errorMessage.includes('NotAllowedError')) {
      setError('カメラの許可が必要です。設定からカメラへのアクセスを許可してください。');
    } else if (errorMessage.includes('NotFoundError')) {
      setError('カメラが見つかりません。デバイスにカメラが接続されているか確認してください。');
    } else {
      setError(`カメラエラー: ${errorMessage}`);
    }
  };
};
```

### QRコード生成設計

```typescript
// QRコード生成ユーティリティ
import QRCode from 'qrcode';

interface QRCodeGenerationOptions {
  size: number;
  margin: number;
  color: {
    dark: string;
    light: string;
  };
}

export const generateOrderQRCode = async (
  orderData: Order,
  options: Partial<QRCodeGenerationOptions> = {}
): Promise<string> => {
  const defaultOptions: QRCodeGenerationOptions = {
    size: 200,
    margin: 2,
    color: {
      dark: '#000000',
      light: '#FFFFFF'
    }
  };

  const config = { ...defaultOptions, ...options };

  // QRコードデータ構造
  const qrData = {
    type: 'pickup',
    orderNumber: orderData.orderNumber,
    timestamp: new Date().toISOString(),
    checksum: generateChecksum(orderData.orderNumber)
  };

  try {
    const qrCodeDataURL = await QRCode.toDataURL(JSON.stringify(qrData), {
      width: config.size,
      margin: config.margin,
      color: config.color
    });

    return qrCodeDataURL;
  } catch (error) {
    console.error('QRコード生成エラー:', error);
    throw new Error('QRコード生成に失敗しました');
  }
};

// チェックサム生成（データ整合性確認用）
const generateChecksum = (orderNumber: string): string => {
  const data = orderNumber + process.env.QR_SECRET_KEY;
  return Buffer.from(data).toString('base64').slice(0, 8);
};
```

### DeliveryPage QRスキャン設計

```typescript
// 受け渡し画面での高度なQRスキャン実装
const DeliveryPage: React.FC = () => {
  const [scanMode, setScanMode] = useState<'qr' | 'manual'>('qr');
  const [currentStream, setCurrentStream] = useState<MediaStream | null>(null);
  
  // BarcodeDetector API使用（Chrome/Edge）
  const initializeBarcodeDetector = useCallback(async () => {
    if ('BarcodeDetector' in window) {
      try {
        const barcodeDetector = new (window as any).BarcodeDetector({
          formats: ['qr_code']
        });
        return barcodeDetector;
      } catch (error) {
        console.warn('BarcodeDetector初期化失敗:', error);
        return null;
      }
    }
    return null;
  }, []);

  // カメラストリーム管理
  const startCameraStream = useCallback(async () => {
    try {
      const stream = await navigator.mediaDevices.getUserMedia({
        video: {
          facingMode: 'environment',
          width: { ideal: 1280 },
          height: { ideal: 720 }
        }
      });
      
      setCurrentStream(stream);
      return stream;
    } catch (error) {
      console.error('カメラ起動エラー:', error);
      throw error;
    }
  }, []);

  // QRコード解析処理
  const processQRCode = useCallback(async (qrData: string) => {
    try {
      const parsedData: QRCodeData = JSON.parse(qrData);
      
      // データ整合性チェック
      if (parsedData.type !== 'pickup') {
        throw new Error('不正なQRコードです');
      }

      // チェックサム検証
      const expectedChecksum = generateChecksum(parsedData.orderNumber);
      if (parsedData.checksum !== expectedChecksum) {
        console.warn('QRコードのチェックサムが不正です');
      }

      // 注文データ取得・処理
      const order = await OrderService.getOrderByNumber(parsedData.orderNumber);
      if (!order) {
        throw new Error(`注文番号 ${parsedData.orderNumber} が見つかりません`);
      }

      // 受け渡し処理
      await handleOrderHandover(order);
      
    } catch (error) {
      console.error('QRコード処理エラー:', error);
      setError(error instanceof Error ? error.message : 'QRコード処理に失敗しました');
    }
  }, []);
};
```

## リアルタイム通信設計

### Socket.io サーバー設計

```typescript
// Socket.ioサーバー設定
import { Server } from 'socket.io';
import { createServer } from 'http';

const server = createServer(app);
const io = new Server(server, {
  cors: {
    origin: allowedOrigins,
    methods: ["GET", "POST", "PUT", "DELETE"],
    credentials: true
  },
  // パフォーマンス設定
  pingTimeout: 60000,
  pingInterval: 25000
});

// Socket.ioイベントハンドラー
export class SocketHandlers {
  constructor(private io: Server) {
    this.setupEventHandlers();
  }

  private setupEventHandlers() {
    this.io.on('connection', (socket) => {
      console.log('クライアント接続:', socket.id);

      // 注文状況更新の通知
      socket.on('join-orders-room', () => {
        socket.join('orders');
      });

      // 厨房専用ルーム
      socket.on('join-kitchen-room', () => {
        socket.join('kitchen');
      });

      // 受け渡し専用ルーム
      socket.on('join-delivery-room', () => {
        socket.join('delivery');
      });

      socket.on('disconnect', (reason) => {
        console.log('クライアント切断:', socket.id, reason);
      });
    });
  }

  // 注文状況変更通知
  public notifyOrderStatusChange(order: Order) {
    this.io.to('orders').emit('order-status-updated', order);
    
    // 厨房への通知
    if (order.status === '注文受付') {
      this.io.to('kitchen').emit('new-order', order);
    }
    
    // 受け渡し画面への通知
    if (order.status === '調理完了') {
      this.io.to('delivery').emit('order-ready', order);
    }
  }

  // 緊急停止通知
  public notifyEmergencyStop(reason: string) {
    this.io.emit('emergency-stop', { reason, timestamp: new Date() });
  }
}
```

### クライアント側Socket.io設計

```typescript
// カスタムHook: useSocket
import { useEffect, useContext } from 'react';
import { io, Socket } from 'socket.io-client';

export const useSocket = () => {
  const { dispatch } = useContext(AppContext);
  const [socket, setSocket] = useState<Socket | null>(null);

  useEffect(() => {
    const socketInstance = io(process.env.REACT_APP_SERVER_URL || 'http://localhost:3001', {
      transports: ['websocket', 'polling']
    });

    // イベントリスナー設定
    socketInstance.on('connect', () => {
      console.log('Socket.io接続成功');
      setSocket(socketInstance);
    });

    socketInstance.on('order-status-updated', (order: Order) => {
      dispatch({ type: 'UPDATE_ORDER', payload: order });
    });

    socketInstance.on('new-order', (order: Order) => {
      dispatch({ type: 'ADD_ORDER', payload: order });
    });

    socketInstance.on('order-ready', (order: Order) => {
      // 受け渡し待ち通知
      dispatch({ type: 'ORDER_READY', payload: order });
    });

    return () => {
      socketInstance.disconnect();
    };
  }, [dispatch]);

  return socket;
};
```

## セキュリティ設計

### 認証・認可設計

```typescript
// JWT認証ミドルウェア
import jwt from 'jsonwebtoken';

interface UserPayload {
  userId: string;
  role: 'admin' | 'staff' | 'customer';
  permissions: string[];
}

const authenticateToken = (req: Request, res: Response, next: NextFunction) => {
  const authHeader = req.headers['authorization'];
  const token = authHeader && authHeader.split(' ')[1];

  if (!token) {
    return res.status(401).json({ error: 'アクセストークンが必要です' });
  }

  jwt.verify(token, process.env.JWT_SECRET!, (err, user) => {
    if (err) {
      return res.status(403).json({ error: 'トークンが無効です' });
    }
    req.user = user as UserPayload;
    next();
  });
};

// 権限チェックミドルウェア
const requirePermission = (permission: string) => {
  return (req: Request, res: Response, next: NextFunction) => {
    if (!req.user?.permissions.includes(permission)) {
      return res.status(403).json({ error: '権限が不足しています' });
    }
    next();
  };
};
```

### データ暗号化設計

```typescript
// 機密データの暗号化
import crypto from 'crypto';

class EncryptionService {
  private readonly algorithm = 'aes-256-gcm';
  private readonly secretKey = process.env.ENCRYPTION_KEY!;

  encrypt(text: string): { encrypted: string; iv: string; tag: string } {
    const iv = crypto.randomBytes(16);
    const cipher = crypto.createCipher(this.algorithm, this.secretKey);
    cipher.setAAD(Buffer.from('order-data'));
    
    let encrypted = cipher.update(text, 'utf8', 'hex');
    encrypted += cipher.final('hex');
    
    const tag = cipher.getAuthTag();
    
    return {
      encrypted,
      iv: iv.toString('hex'),
      tag: tag.toString('hex')
    };
  }

  decrypt(encryptedData: { encrypted: string; iv: string; tag: string }): string {
    const decipher = crypto.createDecipher(this.algorithm, this.secretKey);
    decipher.setAuthTag(Buffer.from(encryptedData.tag, 'hex'));
    decipher.setAAD(Buffer.from('order-data'));
    
    let decrypted = decipher.update(encryptedData.encrypted, 'hex', 'utf8');
    decrypted += decipher.final('utf8');
    
    return decrypted;
  }
}
```

## API設計

### RESTful API設計

```typescript
// 注文API設計
export const ordersApiSpec = {
  // 注文一覧取得
  'GET /api/orders': {
    description: '注文一覧を取得',
    query: {
      status?: OrderStatus;
      page?: number;
      limit?: number;
      sortBy?: 'created_at' | 'updated_at';
      order?: 'asc' | 'desc';
    },
    response: {
      data: Order[];
      pagination: {
        page: number;
        limit: number;
        total: number;
        totalPages: number;
      };
    }
  },

  // 注文作成
  'POST /api/orders': {
    description: '新規注文を作成',
    body: {
      customerId?: string;
      items: CreateOrderItemRequest[];
      specialInstructions?: string;
    },
    response: {
      data: Order;
      qrCode: string;
    }
  },

  // 注文状況更新
  'PATCH /api/orders/:orderNumber/status': {
    description: '注文状況を更新',
    params: { orderNumber: string },
    body: { 
      status: OrderStatus;
      reason?: string;
    },
    response: { data: Order }
  }
};

// QRコード関連API
export const qrCodeApiSpec = {
  // QRコード検証
  'POST /api/qr/verify': {
    description: 'QRコードデータを検証',
    body: {
      qrData: string;
      scanMethod: 'camera' | 'manual';
    },
    response: {
      valid: boolean;
      orderNumber?: string;
      order?: Order;
      error?: string;
    }
  }
};
```

### API応答形式の統一

```typescript
// 統一レスポンス形式
interface ApiResponse<T = any> {
  success: boolean;
  data?: T;
  error?: {
    code: string;
    message: string;
    details?: any;
  };
  meta?: {
    timestamp: string;
    requestId: string;
    version: string;
  };
}

// レスポンスヘルパー
export const createSuccessResponse = <T>(data: T): ApiResponse<T> => ({
  success: true,
  data,
  meta: {
    timestamp: new Date().toISOString(),
    requestId: crypto.randomUUID(),
    version: '1.0.0'
  }
});

export const createErrorResponse = (code: string, message: string): ApiResponse => ({
  success: false,
  error: { code, message },
  meta: {
    timestamp: new Date().toISOString(),
    requestId: crypto.randomUUID(),
    version: '1.0.0'
  }
});
```

## デプロイ設計

### Render デプロイ設定

```yaml
# render.yaml - Infrastructure as Code
services:
  # フロントエンド (Static Site)
  - type: web
    name: unifest-frontend
    env: static
    buildCommand: npm run build
    staticPublishPath: ./dist
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=31536000, immutable
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

  # バックエンド (Web Service)
  - type: web
    name: unifest-backend
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    plan: free  # 750時間/月の無料枠
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: unifest-db
          property: connectionString
    healthCheckPath: /api/health

# データベース
databases:
  - name: unifest-db
    databaseName: unifest_orders
    user: unifest_user
    plan: free  # 1GB、90日間無料
```

### 環境変数設計

```typescript
// 環境変数の型定義
interface EnvironmentVariables {
  // 基本設定
  NODE_ENV: 'development' | 'production' | 'test';
  PORT: number;
  
  // データベース
  DATABASE_URL: string;
  DB_SSL: boolean;
  
  // JWT
  JWT_SECRET: string;
  JWT_EXPIRES_IN: string;
  
  // QRコード
  QR_SECRET_KEY: string;
  
  // Socket.io
  SOCKET_CORS_ORIGIN: string;
  
  // フロントエンド
  FRONTEND_URL: string;
}

// 環境変数検証
const validateEnvironment = (): EnvironmentVariables => {
  const requiredVars = [
    'DATABASE_URL',
    'JWT_SECRET',
    'QR_SECRET_KEY'
  ];

  const missing = requiredVars.filter(key => !process.env[key]);
  if (missing.length > 0) {
    throw new Error(`Missing required environment variables: ${missing.join(', ')}`);
  }

  return {
    NODE_ENV: (process.env.NODE_ENV as any) || 'development',
    PORT: parseInt(process.env.PORT || '3001'),
    DATABASE_URL: process.env.DATABASE_URL!,
    DB_SSL: process.env.NODE_ENV === 'production',
    JWT_SECRET: process.env.JWT_SECRET!,
    JWT_EXPIRES_IN: process.env.JWT_EXPIRES_IN || '24h',
    QR_SECRET_KEY: process.env.QR_SECRET_KEY!,
    SOCKET_CORS_ORIGIN: process.env.FRONTEND_URL || 'http://localhost:5173',
    FRONTEND_URL: process.env.FRONTEND_URL || 'http://localhost:5173'
  };
};
```

## パフォーマンス設計

### React最適化戦略

```typescript
// メモ化による最適化
const OrderCard = React.memo(({ order }: { order: Order }) => {
  const handleStatusUpdate = useCallback((newStatus: OrderStatus) => {
    updateOrderStatus(order.orderNumber, newStatus);
  }, [order.orderNumber]);

  return (
    <Card>
      <CardContent>
        <Typography variant="h6">注文番号: {order.orderNumber}</Typography>
        <OrderStatusChip status={order.status} />
        <Button onClick={() => handleStatusUpdate('調理完了')}>
          調理完了
        </Button>
      </CardContent>
    </Card>
  );
});

// 仮想リスト（大量データ対応）
import { FixedSizeList as List } from 'react-window';

const OrderList: React.FC<{ orders: Order[] }> = ({ orders }) => {
  const ItemRenderer = ({ index, style }: { index: number; style: any }) => (
    <div style={style}>
      <OrderCard order={orders[index]} />
    </div>
  );

  return (
    <List
      height={600}
      itemCount={orders.length}
      itemSize={120}
      width="100%"
    >
      {ItemRenderer}
    </List>
  );
};
```

### データベース最適化

```sql
-- クエリ最適化例
-- 受け渡し待ち注文の効率的取得
CREATE INDEX CONCURRENTLY idx_orders_ready_for_pickup 
ON orders(status, cooking_completion_time) 
WHERE status = '調理完了';

-- 統計情報更新の自動化
CREATE OR REPLACE FUNCTION update_order_statistics()
RETURNS TRIGGER AS $$
BEGIN
    -- 統計テーブルを更新
    INSERT INTO daily_stats (date, total_orders, total_revenue)
    VALUES (CURRENT_DATE, 
           (SELECT COUNT(*) FROM orders WHERE DATE(created_at) = CURRENT_DATE),
           (SELECT SUM(total_amount) FROM orders WHERE DATE(created_at) = CURRENT_DATE))
    ON CONFLICT (date) 
    DO UPDATE SET 
        total_orders = EXCLUDED.total_orders,
        total_revenue = EXCLUDED.total_revenue;
    
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;
```

### キャッシュ戦略

```typescript
// React Query による効率的なデータフェッチング
import { useQuery, useMutation, useQueryClient } from 'react-query';

export const useOrders = (status?: OrderStatus) => {
  return useQuery(
    ['orders', status],
    () => OrderService.getOrders({ status }),
    {
      staleTime: 30000,  // 30秒間はキャッシュを使用
      cacheTime: 300000, // 5分間キャッシュを保持
      refetchInterval: 60000, // 1分ごとに自動更新
      refetchOnWindowFocus: true
    }
  );
};

export const useUpdateOrderStatus = () => {
  const queryClient = useQueryClient();
  
  return useMutation(
    ({ orderNumber, status }: { orderNumber: string; status: OrderStatus }) =>
      OrderService.updateOrderStatus(orderNumber, status),
    {
      onSuccess: (updatedOrder) => {
        // 楽観的更新
        queryClient.setQueryData(['orders'], (old: Order[] | undefined) => {
          return old?.map(order => 
            order.orderNumber === updatedOrder.orderNumber 
              ? updatedOrder 
              : order
          ) || [];
        });
      }
    }
  );
};
```

## エラーハンドリング設計

### フロントエンド エラーハンドリング

```typescript
// エラーバウンダリ
class ErrorBoundary extends React.Component<
  { children: React.ReactNode },
  { hasError: boolean; error?: Error }
> {
  constructor(props: any) {
    super(props);
    this.state = { hasError: false };
  }

  static getDerivedStateFromError(error: Error) {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: React.ErrorInfo) {
    console.error('React Error Boundary:', error, errorInfo);
    
    // エラー報告（本番環境）
    if (process.env.NODE_ENV === 'production') {
      this.reportError(error, errorInfo);
    }
  }

  private reportError = (error: Error, errorInfo: React.ErrorInfo) => {
    // エラー追跡サービスへの送信
    fetch('/api/errors', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        message: error.message,
        stack: error.stack,
        componentStack: errorInfo.componentStack,
        timestamp: new Date().toISOString()
      })
    }).catch(console.error);
  };

  render() {
    if (this.state.hasError) {
      return (
        <Alert severity="error" sx={{ m: 2 }}>
          <AlertTitle>システムエラーが発生しました</AlertTitle>
          申し訳ございませんが、予期しないエラーが発生しました。
          ページを更新してもう一度お試しください。
          <Button 
            onClick={() => window.location.reload()}
            variant="contained"
            sx={{ mt: 1 }}
          >
            ページを更新
          </Button>
        </Alert>
      );
    }

    return this.props.children;
  }
}
```

### バックエンド エラーハンドリング

```typescript
// 包括的エラーハンドリング
export class ErrorService {
  static handleDatabaseError(error: any): AppError {
    if (error.code === '23505') { // Unique constraint violation
      return new AppError('データが重複しています', 409);
    }
    if (error.code === '23503') { // Foreign key constraint violation
      return new AppError('参照エラーが発生しました', 400);
    }
    if (error.code === 'ECONNREFUSED') {
      return new AppError('データベース接続エラー', 503);
    }
    
    console.error('データベースエラー:', error);
    return new AppError('データベース操作に失敗しました', 500);
  }

  static handleValidationError(error: any): AppError {
    const messages = error.details?.map((detail: any) => detail.message).join(', ');
    return new AppError(`入力データが無効です: ${messages}`, 400);
  }

  static logError(error: Error, context: string) {
    const errorLog = {
      timestamp: new Date().toISOString(),
      context,
      message: error.message,
      stack: error.stack,
      level: 'error'
    };

    console.error('エラーログ:', errorLog);
    
    // 本番環境では外部ログサービスに送信
    if (process.env.NODE_ENV === 'production') {
      // 例: Sentry, LogRocket, CloudWatch等への送信
    }
  }
}
```

## 最終更新

- **作成日**: 2024年11月
- **最終更新**: 2024年11月
- **バージョン**: 1.0.0
- **更新内容**: 
  - QRコード機能の詳細設計追加
  - iOS Safari対応の技術仕様明記
  - レート制限緩和の設計追加
  - リアルタイム通信設計の詳細化
  - パフォーマンス最適化戦略の追加